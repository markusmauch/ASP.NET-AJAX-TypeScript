
module Sys.Application
{
    /**
     * Registers with the application an object that will require disposing.
     * This member is static and can be invoked without creating an instance of the class.
     * @param object
     *      The object to register.
     */
    export function registerDisposableObject( object: Sys.Component )
    {
    }
}